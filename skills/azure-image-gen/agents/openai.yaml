interface:
  display_name: "Azure Image Gen"
  short_description: "Generate image files with Azure Image Gen"
  default_prompt: "Use $azure-image-gen to generate an image with my Azure Image Gen deployment and save it in the current project."
policy:
  allow_implicit_invocation: true
